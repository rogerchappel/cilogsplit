# Examples

Try the bundled examples locally:

```sh
npm run build
node dist/cli.js split examples/github-actions.log --format markdown
```

The examples are tiny on purpose so the output is easy to inspect.
